# Development Environment Variables
# คัดลอกไปเป็น .env.local สำหรับ development

DATABASE_URL="postgresql://user:password@localhost:5432/bl1nk_auth?schema=public"

# 🔴 Critical - จำเป็นต้องมี
AUTH_ISSUER=http://localhost:3000
AUTH_AUDIENCE=auth
AUTH_KEY_KID=dev-key-1

# JWT Keys (สร้างด้วย: npm run gen:key)
AUTH_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
AUTH_PUBLIC_KEY_PEM="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# 🟡 Optional
# JWT_SECRET= (เลือกใส่เพื่อเปลี่ยนจาก RS256 เป็น HS256 หรือใช้ทับซ้อน)
